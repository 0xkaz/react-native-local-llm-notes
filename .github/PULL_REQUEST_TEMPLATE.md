## Summary

What does this change and why?

## Changes

-

## Verification

- [ ] `make lint` passes
- [ ] `make test` passes
- [ ] `make test-app` passes
- [ ] `make build` (type-check core + app) passes
- [ ] Verified on a device/simulator (note which), if behavior changed

## Notes

- [ ] Business logic added to `src/core/` is unit-tested and RN-free
- [ ] User-facing strings added in both `en` and `ja` (`src/app/i18n.ts`)
- [ ] No secrets committed; no parent-repo references
