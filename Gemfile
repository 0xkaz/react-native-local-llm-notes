source 'https://rubygems.org'

# Pin Ruby to the version bundled with recent macOS / CI images.
ruby ">= 2.6.10"

# CocoaPods manages the iOS native dependencies (llama.rn, etc.). Match the
# version range used by React Native 0.76's iOS template.
gem 'cocoapods', '>= 1.13', '!= 1.15.0', '!= 1.15.1'
gem 'activesupport', '>= 6.1.7.5', '!= 7.1.0'
